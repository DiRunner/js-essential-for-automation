"use strict";
/**
 * Use case;
 * 1. Prevents any undeclared global variable
 * 2. Use implements, interface, let, package, private, protected, public, static, and yield as indentifier
 * 
 */

//Use Case 1:
a = 1 
console.log(a);

//Use Case 2:

let package = "any package"
console.log(package);